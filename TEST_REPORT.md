# 最近分支覆盖测试报告

## 📋 Git 基准点

- Commit: 644bd09
- 提交说明: Remove unmatched providers during route sync
- 测试日期: 2026-07-24
- 分支: master

## 🎯 本次变更范围

- 能力路由同步仅检查已启用且能力等级与路由匹配的模型。
- 路由开启思考模式时，模型还必须配置路由选择的思考级别才参与同步检查。
- 自动从当前路由移除没有任何有效匹配模型的供应商，但保留供应商和模型主数据。
- 自动移除最后一个供应商且没有旧候选模型时停用路由，避免空池被解释为全局供应商。
- 健康检查失败的匹配模型继续保留在路由中，不触发自动移除。
- 前端同步完成后重新加载路由数据，立即展示后端自动清理后的供应商池。
- 默认能力路由兼容保存和保留思考模式配置。

## 📋 本次变更测试结果（2026-07-24）

**变更范围**：能力及思考级别匹配、无匹配供应商自动移除、空路由停用、前端同步刷新和默认路由思考配置回归。

**测试执行结果**：
- 总测试用例：107 个
- 通过：107 个（100%）
- 失败：0 个
- 错误：0 个
- 跳过：0 个

**关键模块测试**：
- Domain 层：未修改，通过 Service 回归间接覆盖
- Service 层：32/32，通过
- Controller 层：12/12，通过
- 后端基础设施层：3/3，通过
- Python Worker：12/12，通过
- 前端回归：48/48，通过
- 前端生产构建：通过
- Compose 服务健康检查：Backend、Frontend、Python Worker 全部 healthy

**缺陷复现与修复确认**：
- 修复前后端完整测试共 47 项，新增的能力不匹配、思考级别不匹配和全部不匹配场景共 3 项失败，稳定复现同步仍检查无效模型的问题。
- 修复后从纯净 Git 提交 `644bd09` 导出的后端完整测试共 47 项，全部通过。
- 前端同步刷新定向测试 4 项、前端完整回归 48 项和 Python Worker 12 项全部通过。

**Git 基准点**：644bd09

## 📊 测试执行结果

| 测试范围 | 执行命令 | 结果 |
| --- | --- | --- |
| 后端缺陷复现 | `docker build --target build -t base-ai-backend-capability-sync-red ./backend` | 47 项中 3 项失败，稳定复现能力及思考级别未过滤问题 |
| 后端纯净提交完整测试 | `git archive HEAD` 导出临时目录后执行 `docker build --target build -t base-ai-backend-644bd09-test <temp>/backend` | 47 通过，0 失败，0 错误，0 跳过 |
| 前端定向测试 | `node --test frontend/test/model-route-health.test.mjs` | 4 通过，0 失败，0 跳过 |
| 前端完整回归 | `node --test frontend/test/*.test.mjs` | 48 通过，0 失败，0 跳过 |
| Python Worker 回归 | `docker run --rm -v "$PWD/python-worker:/workspace" -w /workspace python:3.12-slim sh -c "pip install --no-cache-dir -q -r requirements.txt pytest && python -m pytest -q"` | Python 3.12；12 通过，0 失败，0 跳过 |
| 服务重建与启动 | `docker compose up --build -d` | 通过；三个服务均重新构建并启动 |
| 前端生产构建 | `docker compose up --build -d`（Frontend 镜像内执行 `npm run build`） | 通过 |
| 服务健康检查 | `docker compose ps` | Backend、Frontend、Python Worker 全部 healthy |
| 差异格式检查 | `git diff --check` | 通过 |

## 🔄 覆盖范围与结果

- 后端正常场景：多个供应商均存在能力匹配模型时，检查全部有效模型并保留供应商池。
- 后端边界场景：部分或全部供应商无匹配能力时，仅检查匹配模型；全部移除且无旧候选模型时停用路由。
- 后端思考场景：开启思考模式后，仅保留至少存在一个匹配能力和思考级别模型的供应商。
- 后端异常与回归：健康检查失败不自动移除；旧请求 `providerIds` 仍被忽略；空配置、旧候选模型、内存快照和手动移除行为保持兼容。
- 前端：覆盖同步请求参数、同步后路由刷新、状态颜色、删除按钮，以及既有 API Key、Trace、任务、布局、导航和分页回归。
- Worker：完整测试通过，本次未修改 Worker 业务代码。
- 运行环境：Compose 已基于最终代码重新构建，Backend、Frontend、Python Worker 均处于 healthy 状态。

## ⚠️ 已知问题与限制

- 供应商被自动移除后，即使后续新增了匹配模型，也需要用户重新将该供应商加入能力路由并再次同步。
- 健康检查失败的匹配模型在用户未删除时仍会进入真实调用候选池，这是已确认的业务行为；实际调用可能继续失败并触发现有候选回退逻辑。
- 旧候选模型路由继续按显式模型列表同步，不执行供应商池自动清理。
- 后端编译仍提示 `TaskTraceService` 存在既有未检查泛型操作，未影响测试结果。
- 前端构建仍有既有 runtime-config、第三方 PURE 注释和包体积警告，不影响构建成功。
- 尚未增加真实浏览器端到端测试，本次前端交互通过源码级 Node 测试和生产构建验证。

## 📝 下次测试建议

1. 补充数据库集成测试，验证自动移除供应商后的事务提交和并发同步行为。
2. 补充浏览器端到端测试，覆盖同步后供应商自动消失、路由停用和重新配置恢复的完整操作链路。
3. 后续修改能力、思考级别、候选回退或模型健康状态逻辑时，基于本报告基准点重新执行完整测试。
4. 评估前端按路由拆分主包，并在合适时处理后端未检查泛型警告。
