# 最近分支覆盖测试报告

## 📋 Git 基准点

- Commit: 883180a
- 提交说明: Fix default route capability sync
- 测试日期: 2026-07-24
- 分支: master

## 🎯 本次变更范围

- `DEFAULT` 路由允许保存用户选择的能力级别，启动和同步初始化不再重置为 `MIDDLE`。
- 路由同步健康检查覆盖供应商下全部启用模型，不再按路由能力级别提前排除。
- 真实内存候选池继续按路由能力级别过滤，避免不同能力模型参与实际调用。
- 补充默认路由能力保存、初始化保留、跨能力健康检查和候选池过滤回归测试。

## 📋 本次变更测试结果（2026-07-24）

**变更范围**：默认路由能力级别可编辑，以及同步健康检查范围修复。

**测试执行结果**：
- 总测试用例：88 个
- 通过：88 个（100%）
- 失败：0 个
- 错误：0 个
- 跳过：0 个

**关键模块测试**：
- Domain 层：未修改
- Service/Controller 等后端层：42/42，通过
- Worker 层：未修改，Compose 健康检查通过
- 前端回归：46/46，通过
- 前端生产构建：通过
- Compose 服务健康检查：通过

**新发现的问题**：
- 本机未安装 Maven，后端完整测试通过 Backend Docker 镜像内的 `mvn -B -ntp package` 执行。
- 修复前新增的 4 项缺陷测试稳定失败；修复后完整后端测试全部通过。
- 前端构建仍有既有 runtime-config、第三方 PURE 注释和包体积警告，不影响构建。

**Git 基准点**：883180a

- `DEFAULT` 仍保持固定编码、不可删除和禁用思考模式，但能力级别可配置。
- 健康检查与实际候选选择分离：前者覆盖供应商全部启用模型，后者继续执行能力匹配。

## 📊 测试执行结果

| 测试范围 | 执行命令 | 结果 |
| --- | --- | --- |
| 服务重建与启动 | `docker compose up --build -d` | 通过；Backend、Frontend、Python Worker 均 healthy |
| Compose 配置校验 | `docker compose config --quiet` | 通过 |
| 后端缺陷复现 | `docker compose build backend` | 修复前新增 4 项测试失败，稳定复现能力覆盖问题 |
| 后端完整测试 | `docker compose up --build -d`（Backend 镜像内执行 `mvn -B -ntp package`） | 42 通过，0 失败，0 错误，0 跳过 |
| Python Worker | `docker compose up --build -d` | 未修改代码；容器健康检查通过 |
| 前端完整回归 | `node --test frontend/test/*.test.mjs` | 46 通过，0 失败，0 跳过 |
| 前端生产构建 | `docker compose up --build -d`（Frontend 镜像内执行 `npm run build`） | 通过 |
| 差异格式检查 | `git diff --check` | 通过 |

## 🔄 覆盖范围与结果

- 后端：默认路由能力保存、启动保留、跨能力健康检查、实际候选能力过滤及既有路由回归测试通过。
- Worker：本次未修改；Compose 启动和健康检查通过。
- 前端：同步状态颜色、删除按钮规则及既有 API Key、Trace、任务、布局、导航和分页回归测试全部通过；生产构建通过。
- 运行环境：Compose 服务已重新构建并启动，三个服务健康检查均通过。

## ⚠️ 已知问题与限制

- 后端编译提示 `TaskTraceService` 存在未检查泛型操作；测试通过，未影响本次结果。
- 前端构建提示运行时配置脚本不会被 Vite 打包、第三方库 PURE 注释位置不可解析，以及主包体积超过 500 kB；均为非阻塞警告，构建成功。
- `DEFAULT` 修改能力级别后需要再次执行同步，新的能力候选才会进入内存快照。

## 📝 下次测试建议

1. 后续修改后端业务逻辑或能力路由同步逻辑时，基于本报告基准点重新执行完整测试。
2. 补充浏览器端到端测试，覆盖修改 `DEFAULT` 能力级别后重新同步和候选切换。
3. 评估前端按路由拆分主包，并在合适时处理后端的未检查泛型警告。
