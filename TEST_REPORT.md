# 最近分支覆盖测试报告

## 📋 Git 基准点

- Commit: 1904814
- 提交说明: Add per-route model provider sync
- 测试日期: 2026-07-24
- 分支: master

## 🎯 本次变更范围

- 能力路由列表增加逐路由同步入口，支持供应商多选，未选择时同步当前路由全部供应商。
- 同步结果按 10 秒、30 秒边界和失败状态展示绿色、黄色、浅红色、深红色反馈。
- 非健康结果支持从当前路由删除对应供应商，数据库路由配置和内存路由快照同步更新，不删除供应商或模型主数据。
- 补充路由范围、空池兼容、删除最后供应商、耗时边界、权限和前端状态映射测试。

## 📋 本次变更测试结果（2026-07-24）

**变更范围**：能力路由逐路由同步、健康结果展示和当前路由模型供应删除。

**测试执行结果**：
- 总测试用例：86 个
- 通过：86 个（100%）
- 失败：0 个
- 错误：0 个
- 跳过：0 个

**关键模块测试**：
- Domain 层：未修改
- Service/Controller 等后端层：40/40，通过
- Worker 层：未修改，Compose 健康检查通过
- 前端回归：46/46，通过
- 前端生产构建：通过
- Compose 服务健康检查：通过

**新发现的问题**：
- 本机未安装 Maven，后端完整测试通过 Backend Docker 镜像内的 `mvn -B -ntp package` 执行。
- 首次重建时 `8080` 被无关项目占用；释放端口后最终 Compose 重建、启动和健康检查均通过。
- 前端构建仍有既有 runtime-config、第三方 PURE 注释和包体积警告，不影响构建。

**Git 基准点**：1904814

- 同步和删除操作均限定到每条能力路由；供应商多选为空时只同步该路由全部模型供应。
- 删除操作仅清理当前路由关联并刷新内存快照，供应商与模型主数据保持不变。

## 📊 测试执行结果

| 测试范围 | 执行命令 | 结果 |
| --- | --- | --- |
| 服务重建与启动 | `docker compose up --build -d` | 通过；Backend、Frontend、Python Worker 均 healthy |
| Compose 配置校验 | `docker compose config --quiet` | 通过 |
| 后端完整测试 | `docker compose up --build -d`（Backend 镜像内执行 `mvn -B -ntp package`） | 40 通过，0 失败，0 错误，0 跳过 |
| Python Worker | `docker compose up --build -d` | 未修改代码；容器健康检查通过 |
| 前端完整回归 | `node --test frontend/test/*.test.mjs` | 46 通过，0 失败，0 跳过 |
| 前端生产构建 | `docker compose up --build -d`（Frontend 镜像内执行 `npm run build`） | 通过 |
| 差异格式检查 | `git diff --check` | 通过 |

## 🔄 覆盖范围与结果

- 后端：逐路由供应商筛选、全供应商空池、旧候选模型、删除关联、内存刷新、权限和耗时边界测试通过。
- Worker：本次未修改；Compose 启动和健康检查通过。
- 前端：同步状态颜色、删除按钮规则及既有 API Key、Trace、任务、布局、导航和分页回归测试全部通过；生产构建通过。
- 运行环境：Compose 服务已重新构建并启动，三个服务健康检查均通过。

## ⚠️ 已知问题与限制

- 后端编译提示 `TaskTraceService` 存在未检查泛型操作；测试通过，未影响本次结果。
- 前端构建提示运行时配置脚本不会被 Vite 打包、第三方库 PURE 注释位置不可解析，以及主包体积超过 500 kB；均为非阻塞警告，构建成功。
- 路由升级继续兼容旧候选模型 ID 模式；删除路由最后一个供应商时会停用该路由，避免空池重新解释为全部供应商。

## 📝 下次测试建议

1. 后续修改后端业务逻辑或能力路由同步逻辑时，基于本报告基准点重新执行完整测试。
2. 补充浏览器端到端测试，覆盖真实慢响应、失败模型和删除确认交互。
3. 评估前端按路由拆分主包，并在合适时处理后端的未检查泛型警告。
